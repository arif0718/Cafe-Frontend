const Home = ({ name, age }) => {
    return (
        <div>
            <h1>Hello {name}. You are {age} years old. </h1>
            <p>This is a paragraph</p>
        </div>
    )
}

export default Home